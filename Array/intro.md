An array is a data structure that allows you to store a sequence of values, all of the **same type**.

An array is a special class in Java. It's still a class.

The array, like all other classes, ultimately inherits from 
```java.lang.Object```
it's still a class.

```java
int [ ] intergerArray --> more common
String courseList[]
```