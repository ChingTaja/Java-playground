1. as a form of code reuse
2. It's a way to organize **classes** into a **parent-child hierarchy**, which lets the child inherit (in other words reuse), fields and methods from its parent.

Parent class 
        ↑
        |
Child class 