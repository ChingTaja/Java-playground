多種形式

優點

1.  讓程式碼更簡單

我們可以只用 父類別（base class 或 superclass） 寫一次程式碼

就像在 doAnimalStuff 方法裡做的那樣。

寫這段程式時 完全不需要知道子類別的型別。

我不需要寫程式碼去：檢查物件的型別

再決定要呼叫哪一個方法

Java 會在執行時（runtime）自動幫我們完成這件事。

2. 讓程式更容易擴展

多型也鼓勵 程式碼的 extensibility

建立新的子類別非常容易，你只需要：繼承父類別

覆寫（override）方法

或擴展原本的方法
