List 必須可以動態新增 / 刪除元素

List 在 Java 中：
- 是一種 Interface（介面）==> 不負責實作
-  不是具體類別（不能直接 new）

> List 是「規則書」，ArrayList / LinkedList 是「實作者」
> List 是 Java 中的 Interface，定義了操作集合的方法（如 add、remove），而 ArrayList 與 LinkedList 是它的實作類別